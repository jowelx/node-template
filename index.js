import startServer from "./src/app.js";

//Aca va tu funcion que conecta la base de datos .then(connectionResult => {
 // if (connectionResult) {
    startServer();
//  } else {
//    throw new Error(ERR_DB);
// }
//});